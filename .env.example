# Database Configuration
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database_name
DATABASE_URL="postgresql://postgres:password@localhost:5432/time_management_matrix"

# Authentication Configuration
# Better Auth secret key - use a strong, random string in production
# You can generate one using: openssl rand -base64 32
BETTER_AUTH_SECRET="your-super-secret-key-change-this-in-production"

# Better Auth URL - your application's base URL
BETTER_AUTH_URL="http://localhost:3000"

# Next.js Authentication URL (usually same as BETTER_AUTH_URL)
NEXTAUTH_URL="http://localhost:3000"

# Optional: Disable Next.js telemetry
# NEXT_TELEMETRY_DISABLED=1

# Development Configuration (Optional)
# Set to development for local development
NODE_ENV="development"

# Production Configuration Examples
# Uncomment and modify for production deployment

# Production Database (e.g., for Vercel, Railway, PlanetScale)
# DATABASE_URL="postgresql://username:password@host:port/database_name?sslmode=require"

# Production URLs
# BETTER_AUTH_URL="https://your-domain.com"
# NEXTAUTH_URL="https://your-domain.com"

# Security Note:
# - Never commit your actual .env file to version control
# - Always use strong, unique secrets for production
# - Consider using environment-specific configurations
# - For production, use secure password management systems
